name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

env:
  DATABASE_URL: "sqlite+aiosqlite:///:memory:"
  REDIS_URL: redis://localhost:6379/0
  CELERY_BROKER_URL: redis://localhost:6379/0
  CELERY_RESULT_BACKEND: redis://localhost:6379/1

jobs:
  lint-and-test:
    name: Lint & unit tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Ruff
        run: ruff check app tests

      - name: Pytest (unit only)
        run: pytest tests -m "not integration" -v --cov=app --cov-report=term-missing --cov-fail-under=80

  docker:
    name: Docker build & E2E smoke test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build images
        run: docker compose build

      - name: Start stack
        run: docker compose up -d

      - name: Wait for API health
        run: |
          for i in $(seq 1 30); do
            if curl -sf http://localhost:8000/health; then
              exit 0
            fi
            sleep 5
          done
          echo "API health check failed"
          docker compose logs
          exit 1

      - name: Verify worker is running
        run: docker compose ps worker | grep -q "running\|Up"

      - name: E2E job pending to succeeded
        run: bash scripts/e2e-smoke.sh

      - name: Tear down
        if: always()
        run: docker compose down -v
